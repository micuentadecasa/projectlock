# Use the official OpenProject image as a base
FROM openproject/community:latest

# Expose the port OpenProject listens on
EXPOSE 8080

# Note: Configuration for OpenProject should mainly be done through the .env file.
# If you have additional scripts for setup, add them here